using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ControlManager : MonoBehaviour
{
    //Here we can check if some UI Panels are opended or not
    //For example we can't move if Panel Tree is open

    public static bool canvasTree = false;
    public static bool canvasPlayer = false;
    public static bool battle = false;

    
}
